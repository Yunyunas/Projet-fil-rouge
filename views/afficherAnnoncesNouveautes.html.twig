{% extends "layout.html.twig" %}

{% block title %} <title> Rubrique {{paramLibelle}} </title> {% endblock %}
{% block content %}

<body>
<!-- Page Content -->
<div class="container">

  <!-- Page Heading -->
    <h1 class="my-4">Rubrique {{paramLibelle}}</h1>

    {# <a class="btn btn-danger" href="index.php?action=formAjouterAnnonce" style="visibility:{{ session.utilisateur.id_utilisateur != '' ? 'visible' : 'hidden' }}">Ajouter une annonce</a> #}

    <div class="row">
        <div class="col-lg-3">
            <div class="list-group">
                <a href="index.php?action=afficherAnnoncesNouveautes&param=1&paramLibelle=Nouveautés" class="list-group-item active">Nouveautés</a>
                
                {% for rub in rubrique %}

                <a href="?action=afficherAnnonces&param={{rub.id}}&paramLibelle={{rub.libelle}}" class="list-group-item">{{rub.libelle}}</a>

                {% endfor %}
            </div>
        </div>



        <div class="col-lg-9">
     
        <!-- /.col-lg-3 -->
            
            <div class="row">
                {% for a in annonce %}
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="card h-100">
                            <a href="?action=afficherUneAnnonce&paramEnTete={{a.entete}}&paramCorps={{a.corps}}&paramIdAnnonce={{a.id}}">
                                <img class="card-img-top" src="public/images/article1.jpg" alt="">
                            </a>
                            <div class="card-body">
                                <h4 class="card-title">
                                    <a href="?action=afficherUneAnnonce&paramEnTete={{a.entete}}&paramCorps={{a.corps}}&paramIdAnnonce={{a.id}}">{{a.entete}}</a>
                                </h4>
                                <h5>100.00 €</h5>
                                <p class="card-text">{{a.corps}}</p>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
	</div>
</div>  

  <!-- /.row -->

  {# <!-- Pagination -->
  <ul class="pagination justify-content-center">
    <li class="page-item">
      <a class="page-link" href="#" aria-label="Previous">
            <span aria-hidden="true">&laquo;</span>
            <span class="sr-only">Previous</span>
          </a>
    </li>
    <li class="page-item">
      <a class="page-link" href="#">1</a>
    </li>
    <li class="page-item">
      <a class="page-link" href="#">2</a>
    </li>
    <li class="page-item">
      <a class="page-link" href="#">3</a>
    </li>
    <li class="page-item">
      <a class="page-link" href="#" aria-label="Next">
            <span aria-hidden="true">&raquo;</span>
            <span class="sr-only">Next</span>
          </a>
    </li>
  </ul> #}

</div>
<!-- /.container -->
</body>
{% endblock %}